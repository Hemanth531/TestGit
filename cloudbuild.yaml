steps:
  - name: 'gcr.io/cloud-builders/gcloud'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        gcloud compute ssh hemanth-vm1 --zone=us-central1-a --command="sudo apt-get update && sudo apt-get install -y openjdk-17-jdk"

options:
  logging: CLOUD_LOGGING_ONLY

